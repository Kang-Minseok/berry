# 결과 값을 저장하는 목록.
result = []
# 전체 범위를 지정.
# 초기값은 별도로 생성할 것이므로 전체 범위는 3부터 1000까지.
a = range(3,1001)
# 초기값 설정.
base = 2
# 초기값의 배수를 구하기 위한 임시 변수를 생성.
p = base
# "에라토스테네스의 체" 알고리즘을 수행.
while a:
    # 임시변수가 1000일 때까지 수행한다.
    while p < 1001:
        # 임시변수가 전체 범위내 존재한다면
        if p in a:
            # a에서 임시변수를 삭제.
            a.remove(p)
        # 임시변수는 기본값의 배수.
        p = p + base
    # 결과 목록에 기본값을 추가.
    result.append(base)
    # 다음 기본값은 (이미 걸러진)전체 목록의 첫 번째 값. 
    # 1회 걸렀을 경우 2와 2의 배수를 모두 삭제했으므로 3.
    base = a[0]
    #초기값의 배수를 구하기 위해 임시 변수를 다시 생성.
    p = base
    #전체 범위에서 초기값을 제거.
    del a[0]
# 범위 내의 마지막 기본값을 결과 목록에 추가.
result.append(base)
# 출력.
print result
print sum(range(len(result)))
